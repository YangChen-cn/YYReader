# YYReader 1.0.0

首个公开版本。YYReader 是一款仅面向 Apple 芯片、以正文为中心的原生 macOS 小说阅读器。

## 主要功能

- 输入章节网页 URL，自动识别书名、作者、章节标题、正文、目录和前后章节。
- 自动抓取并合并同一章节的多个分页。
- 使用 SwiftUI 原生正文视图，支持文字选择、连续滚动和阅读位置恢复。
- 提供多书书架、目录搜索、章节缓存、离线阅读和下一章预取。
- 提供系统、白色、轻暖纸张和夜间主题，以及字体、字号、行距、正文宽度和段首缩进设置。
- 阅读模式默认突出正文；目录、外观设置和书籍管理操作按需显示。

## 网站加载与解析

- `qidiy.com` 使用专用解析器，支持章节分页和多页目录。
- 其他站点使用基于结构化元数据、语义标签和正文密度的通用解析器。
- Cloudflare 网站触发验证后，同一域名持续复用 WebKit 会话，避免每个分页重复验证。
- HTTP 429 不会自动重试；目录刷新支持取消和超时，当前章节成功后不再依赖完整目录下载。
- 目录保持网站 DOM 顺序，章节标题数字只用于辅助识别，避免正文与番外重新编号时错乱。

## 阅读体验

- 书架模式中，单击章节即可在右侧预览；双击、按 Return 或点击“继续阅读”才进入沉浸阅读。
- 沉浸阅读默认隐藏书架，目录可通过系统侧栏按钮展开。
- 工具栏移除了重复的书名、搜索框和章节导航按钮。
- 章节搜索框只出现在目录侧栏中。
- 返回书架后继续保留当前书籍、章节与阅读位置。

## 系统要求与安装说明

- macOS 15.0 或更高版本。
- Apple 芯片 Mac，仅提供 arm64 构建。
- 本版本使用 ad-hoc 签名，未经过 Apple Developer ID 签名或公证。首次启动时可能需要在 Finder 中右键 YYReader 并选择“打开”。

## 已知限制

- 不自动解决 CAPTCHA，不绕过登录、付费墙或网站访问控制。
- 第三方网站结构和反自动化策略可能变化，通用解析无法保证支持所有站点。
- 首版不包含整本批量下载、全文搜索、书签、高亮、批注、同步和 EPUB 导出。
