# YYReader 1.2.0

YYReader 1.2.0 同时提供原生 macOS 与 Windows 客户端，并加入可选的跨平台书架与阅读位置交换。两个客户端都保持原生正文渲染，不同步正文缓存、Cookie 或网页验证状态。

## macOS 1.2.0

### 文件夹同步与手动传输

- 新增文件夹自动同步。用户可选择 iCloud Drive、Dropbox、OneDrive、Syncthing 或其他任意共享目录，不绑定具体云盘。
- 使用 SyncSnapshot v2 与 Windows 交换书籍元数据、章节序号和阅读位置；Mac 只写 `mac.json`、读取 `windows.json`。
- 新增 `.yyreader`/JSON 文件及剪贴板的书架导入、导出，导入前会预览新增、已有、无效和重复条目。
- 书籍按 canonical source URL 合并；阅读位置只允许由目录中更后的章节或同章更后的段落覆盖。

### 同步稳定性

- 修复 iCloud Drive、OneDrive、Dropbox、NAS 或 File Provider 不可用时可能阻塞应用启动和前台恢复的问题；YYReader 会先显示本地书架并保持 Reader 可用，再在后台恢复权限和尝试同步。
- 本地书架与阅读进度变化走独立发布路径，只构建 Mac 快照并更新 `mac.json`，不会读取或解析 `windows.json`。
- 仅在启动、回到前台、手动同步或检测到 Windows 文件确实变化时执行完整合并。
- Windows 文件 signature 只在读取、合并和落库全部成功后确认；读取失败后 watcher 与低频轮询仍可重试。
- 远端 tombstone 不会立即破坏当前 Reader session；正在阅读的书会在退出阅读后再安全刷新。
- 修复同步触发跳章、旧章节覆盖新章节、后台重复同步及目录暂不可访问时错误清空本地状态的问题。
- 修复无完整目录的小说在连续阅读章末查询下一章时修改界面状态、导致 Reader 偶发闪退的问题。

### macOS 安装

- 支持 Apple 芯片 Mac，要求 macOS 15 或更高版本。
- 下载 `YYReader-1.2.0-arm64.dmg`，打开后将 YYReader 拖入 Applications。
- 使用 ad-hoc 签名且未经过 Apple 公证；首次启动时可能需要在 Finder 中右键应用并选择“打开”。

### macOS 验证

- 全量单元与回归测试：90 项全部通过。
- Release 为纯 arm64、版本 1.2.0（build 5），App Sandbox 保留用户选择文件夹读写和出站网络权限。
- ad-hoc 签名、DMG 校验和、挂载后签名、应用图标、资源及便携性检查全部通过。

## Windows 1.2.0

YYReader 1.2.0 是首个可正式分享的原生 Windows 版本。它使用 C#、.NET 8、WinUI 3、Windows App SDK 和 SQLite，延续 macOS 版“原生、简洁、流畅、适合长期阅读”的核心方向。

## 阅读与书架

- 新增 Windows 11 风格书架、继续阅读区域、目录侧栏和统一自定义标题栏。
- 正文使用 WinUI 原生文本渲染，支持连续滚动、连续章节、阅读位置恢复、键盘翻页和稳定的段落锚点。
- 提供字号、字体、行距、段距、正文宽度、段首缩进及日间、夜间等阅读主题。
- 阅读位置使用章节 URL、段落索引和进度比例恢复；排版变化后保持在当前段落附近。

## 导入、目录与离线阅读

- 支持从小说 URL 导入，包含 qidiy.com 专用适配器和通用 HTML 解析器。
- JavaScript、Cookie 和网站验证由 WebView2 处理，提取后的章节仍在原生阅读器中显示。
- 支持完整目录分页刷新、章节搜索、前后章节发现和下一章后台预取。
- 支持下载当前章节、后续 20 章或全书；下载可取消，已完成章节继续保存在 SQLite。
- 章节正文按需从 SQLite 加载，避免整书缓存后在启动时一次性进入内存。

## Mac 与 Windows 数据交换

- 支持 BookshelfTransfer JSON、`.yyreader` 文件及剪贴板批量导入，并按 canonical source URL 去重。
- 支持书架导出到剪贴板或 `.yyreader` 文件。
- 新增 SyncSnapshot v2 文件夹同步：Windows 只写 `windows.json`、读取 `mac.json`，按更新时间合并元数据、章节号和阅读位置。
- 同步不会在用户阅读过程中重建正文、切换屏幕章节或覆盖本机刚产生的 UI 状态。

## 稳定性与性能

- 修复连续阅读章末抖动、失败后不能重试、下一章尚未加载时出现空白及切章不回标题等问题。
- ReaderItem 改为轻量结构并批量追加，正文缓存保持有界，同时优先维持稳定滚动几何。
- 目录刷新、预取、离线下载和同步均避开滚动热路径及 UI 线程中的网络、解析和 SQLite 写入。
- 修复后台目录刷新阻塞打开小说、标题栏控件双击误触最大化、系统主题切换后已显示正文颜色未更新等问题。

## 安装与打包

- 提供 `YYReader-Setup-x64-1.2.0.exe` 中文完整安装包，包含 .NET 8 与 Windows App Runtime。
- 安装后创建开始菜单入口，可选择桌面快捷方式，并支持在 Windows“已安装的应用”中卸载。
- 重新生成标准多分辨率 Windows 图标，修复安装程序、应用和快捷方式显示白纸图标的问题。
- Windows App SDK 改为按组件引用，移除应用未使用的 AI/ML、ONNX Runtime 和 DirectML 依赖。

## 验证

- Windows Core 单元测试：45 项全部通过。
- Release 构建：0 warning、0 error。
- 完整安装、快捷方式图标、已安装应用启动和自包含运行环境已完成本机验证。

## 系统要求与已知限制

- x64 Windows 10 1809 或更高版本，推荐 Windows 11。
- 安装程序尚未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。
- 不自动解决 CAPTCHA，不绕过登录、付费墙或网站访问控制；第三方网站结构变化可能影响解析。

---

# YYReader 1.1.2

YYReader 1.1.2 聚焦阅读位置恢复、正文渲染性能和可缩放排版。本版本仅提供 Apple 芯片 arm64 DMG。

## 修复

- 修复“继续阅读”无视保存位置、总是回到章首的问题；`.restore` 会恢复保存的顶部段落，`.chapterTop` 才会回到章节标题。
- 修复高级阅读设置缺少退出入口的问题；新增“完成”按钮，并支持 Escape 关闭 Inspector。

## 性能与稳定性

- 新增 8 章容量的 LRU 段落缓存，以章节 ID、缓存时间和内容版本区分缓存；读远后自动淘汰，避免永久保存整本小说的段落快照。
- 写入章节正文时完成 trim/filter，渲染读取阶段只进行轻量 split。
- LibraryStore 建立章节和位置索引，章节选择、邻章查询和进度更新避免反复线性扫描。
- 可见章节目标按目录顺序和段落索引显式排序，连续阅读边界和预取挂接更加稳定。
- Debug 与 Release 脚本在产物验证后清理 DerivedData 中间 App，减少本地构建残留。

## 阅读体验

- 正文宽度改为随字号缩放的 em 模型，预设为 38/48/58em，并提供 20～80em 的自定义滑块。
- 行距和段距改为相对字号比例，字号变化时保持阅读密度一致。
- 连续章节使用更紧凑的标题和边界；纸书主题保留装饰，其余主题使用淡化分隔线。
- 关于页更新为本版本的性能、稳定性和阅读体验说明。

## 验证与安装

- 完整测试：69 个代码测试和 1 个 UI 测试通过。
- Release 构建：arm64，ad-hoc 签名，DMG 校验和、挂载后签名和资源检查通过。
- 下载 `YYReader-1.1.2-arm64.dmg`，打开后将 YYReader 拖入 Applications。
- 本版本未经过 Apple Developer ID 签名或公证；首次启动时可能需要在 Finder 中右键 YYReader 并选择“打开”。

---

# YYReader 1.1.0

YYReader 1.1.0 聚焦连续阅读、离线下载和通用小说网站兼容性。本版本仅提供 Apple 芯片 arm64 DMG。

## 新功能

- 新增可选的连续章节阅读，进入当前章节后立即后台预取下一章，接近章节底部时再安全加入阅读视图。
- 新增“下载到本地”，支持下载当前章节、后续 20 章或整本目录；已缓存章节自动跳过，下载可取消并可隐藏到后台。
- 新增轻量阅读进度，显示当前章节位置和本章阅读百分比。
- 设置新增“关于”页面，显示应用版本、作者、GitHub 链接和本版更新日志。

## 阅读体验

- 连续阅读保持稳定的章节视图结构，不在滚动过程中删除上方整章。
- 章节连接区域使用固定高度，加载状态变化不再改变正文布局。
- 一次滚动事务最多进入下一章，修复惯性滚动跨越多个章节的问题。
- 修复连续换章时目录 selection 反向触发 Reader 重置所造成的闪烁。
- 支持方向键逐段滚动，目录打开时自动居中当前章节。
- 重新打开应用后恢复上次阅读的书籍和章节。

## 网站兼容性

- 支持直接从书籍目录页导入小说。
- 支持没有目录的小说，并使用稳定的书籍级身份避免重复书籍。
- 改进章节页与目录页判断，避免侧栏章节链接导致正文页被误判。
- 支持按 DOM 顺序合并多个同级卷或章节区块。
- 静态 HTML 解析失败时，可复用持久 WebKit 会话获取 rendered DOM 后重试；只有成功解决解析问题才升级该网站的加载偏好。
- 保留 `qidiy.com` 专用适配器和 Cloudflare 持久会话。

## 系统要求与安装

- macOS 15.0 或更高版本。
- 仅支持 Apple 芯片 Mac（arm64）。
- 下载 `YYReader-1.1.0-arm64.dmg`，打开后将 YYReader 拖入 Applications。
- 本版本使用 ad-hoc 签名，未经过 Apple Developer ID 签名或公证。首次启动时可能需要在 Finder 中右键 YYReader 并选择“打开”。

## 项目

- 作者：Yangchen
- GitHub：https://github.com/YangChen-cn/YYReader

## 已知限制

- 不自动解决 CAPTCHA，不绕过登录、付费墙或网站访问控制。
- 第三方网站结构和反自动化策略可能变化，通用解析无法保证支持所有站点。
