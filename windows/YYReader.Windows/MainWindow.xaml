<Window
    x:Class="YYReader.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="YYReader">
    <Grid x:Name="RootGrid" RowDefinitions="Auto,*">
        <Grid x:Name="AppTitleBar" Height="{StaticResource YYTitleBarHeight}" Padding="12,0,8,0" ColumnDefinitions="Auto,*">
            <Grid x:Name="TitleBarDragRegion" Grid.ColumnSpan="2" Background="Transparent" />
            <StackPanel x:Name="TitleBarBrand" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center"
                        IsHitTestVisible="False">
                <Image Source="/Assets/AppIcon.png" Width="18" Height="18" />
                <TextBlock Text="YYReader" VerticalAlignment="Center" FontWeight="SemiBold" />
            </StackPanel>
            <ContentPresenter x:Name="PageTitleBarContent" Grid.Column="1"
                              HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center" />
        </Grid>
        <Frame x:Name="ContentFrame" Grid.Row="1" />

        <Grid x:Name="VerificationOverlay"
              Visibility="Collapsed"
              Grid.RowSpan="2"
              Background="#CCF7F7F7"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid RowDefinitions="Auto,Auto,*,Auto" Width="820" Height="640">
                    <TextBlock Text="网站验证"
                               FontSize="20"
                               FontWeight="SemiBold" />
                    <TextBlock Grid.Row="1"
                               Margin="0,8,0,12"
                               Text="请只在此窗口完成网站要求的验证。验证状态仅保存在本机，不会同步到 Mac。"
                               TextWrapping="WrapWholeWords" />
                    <WebView2 x:Name="VerificationWebView" Grid.Row="2" />
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,12,0,0">
                        <Button Content="取消" Click="CancelVerification_Click" />
                        <Button Content="我已完成验证" Click="CompleteVerification_Click" Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
